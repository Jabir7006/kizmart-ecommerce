services:
  backend:
    image: jabir7006/kizmart-backend:latest
    ports: 
      - "3000:3000"
    env_file:
      - ./backend/.env
  frontend:
    image: jabir7006/kizmart-frontend:latest
    ports:
      - "5173:80"
    env_file:
      - ./frontend/.env